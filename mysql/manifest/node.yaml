apiVersion: v1
kind: Service
metadata:
  name: mysql-nodeport
  namespace: mysql
spec:
  type: NodePort
  ports:
    - port: 8095
      targetPort: 8095      
      nodePort: 31263
  selector:
    app: user-app